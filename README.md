# PyTuxHeist 🪰

![20260218-0548-10 9694793](https://github.com/user-attachments/assets/5662e535-d408-45fe-9795-6d700dc1ce78)
![20260218-0551-26 6683674](https://github.com/user-attachments/assets/8cb9e48c-f603-47f1-8da8-2ac60029e545)
<img width="1280" height="719" alt="image" src="https://github.com/user-attachments/assets/b92434a3-f2a6-4df5-a758-8b6121aa4fa3" />

> **Пранк-приложение для Windows**: анимированные персонажи бегают по рабочему столу, «крадут» ярлыки, а мышь начинает жить своей жизнью.

---

## 🎬 Что происходит при запуске

1. Все окна сворачиваются (`Win+D`)
2. Делается скриншот рабочего стола
3. Скриншот отображается поверх всего экрана — **фейковый рабочий стол**
4. Под скриншотом скрыто изображение `assets/kong.jpg`
5. По экрану начинают бегать **хазбик** и **муха**
6. хазбик находит ярлыки, «крадут» их (вырезают кусок скриншота) и убегают за край экрана
7. **Мышь инвертирована**: движение влево → курсор идёт вправо, вверх → вниз
8. Каждый клик мышью стирает блок `512×512 px` из скриншота, постепенно открывая `kong.jpg`
9. Выйти можно только через пароль

---

## 🕹️ Управление

| Действие | Результат |
|---|---|
| Движение мышью | Инвертировано (влево↔вправо, вверх↔вниз) |
| ЛКМ / ПКМ | Стирает блок 512×512 вокруг курсора |
| Клавиша `=` | Появляется диалог ввода пароля |
| Пароль `12021` | Выход из приложения |

> ⚠️ Закрыть окно через Alt+F4, крестик или Escape **невозможно** — только через пароль.

---

## 🐧 Логика персонажей

### Пингвин (`penguin.py`)
Стейт-машина с четырьмя состояниями:

```
IDLE → WALK → STEAL → EXIT → (исчезает)
```

- **IDLE** — появляется с края экрана, ждёт ~1 сек
- **WALK** — бежит к ближайшему ярлыку (или случайной точке)
- **STEAL** — вырезает кусок скриншота и держит его над головой (~2 сек)
- **EXIT** — убегает за край экрана с «украденным» ярлыком

Спрайт (`assets/kid_sprite.png`) — сетка 3×3, автоматически зеркалится при движении влево.

### Муха (`fly.py`)
- Блуждает по экрану случайным образом
- **Убегает от курсора** при приближении (радиус 150 px)
- В панике (< 70 px) ускоряется в 1.8×
- Поворачивается в направлении движения
- Спрайт (`assets/fly.png`) — сетка 2×2, 4 кадра

---

## 🚀 Установка и запуск

```bash
# Клонировать репозиторий
git clone <repo-url>
cd PyTuxHeist

# Установить зависимости
pip install -r requirements.txt

# Запустить
python main.py
```

> **Требуется Python 3.10+** и **Windows** (используются WinAPI-хуки).

---

## 📦 Зависимости

| Пакет | Назначение |
|---|---|
| `PyQt6` | GUI, графическая сцена, сигналы |
| `mss` | Быстрый захват скриншота |
| `Pillow` | Обработка изображений |
| `pywin32` | Управление окнами Windows |

---

## 📁 Структура проекта

```
PyTuxHeist/
├── main.py            # Точка входа: запускает overlay + инвертор мыши
├── overlay.py         # Полноэкранное окно (фейковый рабочий стол + kong.jpg)
├── mouse_inverter.py  # Low-level mouse hook: инверсия + перехват кликов
├── penguin.py         # Класс Penguin — анимация и стейт-машина
├── fly.py             # Класс Fly — муха, убегающая от курсора
├── utils.py           # Утилиты: скриншот, позиции ярлыков, блокировка Win-клавиши
├── requirements.txt
└── assets/
    ├── kid_sprite.png # Спрайт-лист пингвина (3×3)
    ├── fly.png        # Спрайт-лист мухи (2×2)
    └── kong.jpg       # Фоновое изображение под скриншотом
```

---

## 🛠️ Технические детали

- **Low-level mouse hook** (`WH_MOUSE_LL`) перехватывает все события мыши в отдельном потоке
- **Инверсия** реализована через `SetCursorPos`: при каждом `WM_MOUSEMOVE` курсор перемещается в зеркальную позицию
- **Overlay** — `QMainWindow` без рамки поверх всех окон (`WindowStaysOnTopHint`), с прозрачным фоном
- **Стирание скриншота** — `QPainter` с режимом `CompositionMode_Clear` делает пиксели прозрачными, открывая `kong.jpg`
- **Клавиша выхода** опрашивается таймером Qt каждые 100 мс через `GetAsyncKeyState`

---

> 🔒 Ваши реальные файлы в полной безопасности — приложение только **симулирует** кражу, работая со скриншотом.
